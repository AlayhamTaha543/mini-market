<Window x:Class="MiniMarketApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:MiniMarketApp.ViewModels"
        mc:Ignorable="d"
        Title="{DynamicResource MainWindowTitle}"
        Height="320"
        Width="520">
    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource MainWindowTitle}"
                   FontSize="20"
                   FontWeight="Bold" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="{DynamicResource PosButtonText}" Margin="0,0,8,0">
                <Button.Command>
                    <Binding Path="NavigateCommand" />
                </Button.Command>
                <Button.CommandParameter>
                    <viewModels:AppScreen>PointOfSale</viewModels:AppScreen>
                </Button.CommandParameter>
            </Button>

            <Button Content="{DynamicResource ReportsButtonText}" Margin="0,0,8,0">
                <Button.Command>
                    <Binding Path="NavigateCommand" />
                </Button.Command>
                <Button.CommandParameter>
                    <viewModels:AppScreen>Reports</viewModels:AppScreen>
                </Button.CommandParameter>
            </Button>
        </StackPanel>

        <TextBlock Grid.Row="2" Margin="0,16,0,0" Text="{Binding StatusMessage}" Foreground="DarkRed" />
    </Grid>
</Window>
